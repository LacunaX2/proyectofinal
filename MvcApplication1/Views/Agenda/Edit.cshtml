@model MvcApplication1.Models.tbagenda

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>tbagenda</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.cod_agenda)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.cod_agenda)
            @Html.ValidationMessageFor(model => model.cod_agenda)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.cod_usuario, "tbusuario")
        </div>
        <div class="editor-field">
            @Html.DropDownList("cod_usuario", String.Empty)
            @Html.ValidationMessageFor(model => model.cod_usuario)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.fecha)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.fecha)
            @Html.ValidationMessageFor(model => model.fecha)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
